import { PagePlaceholder } from '../components/PagePlaceholder'

export function HistoryPage() {
  return (
    <PagePlaceholder
      title="History"
      description="Scan history placeholder listing prior analyses and usage details for signed-in users."
    />
  )
}
