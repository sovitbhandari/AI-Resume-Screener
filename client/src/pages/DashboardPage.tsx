import { PagePlaceholder } from '../components/PagePlaceholder'

export function DashboardPage() {
  return (
    <PagePlaceholder
      title="Dashboard"
      description="Primary workflow page where users upload resume PDFs and provide job descriptions."
    />
  )
}
