import { PagePlaceholder } from '../components/PagePlaceholder'

export function LoginPage() {
  return (
    <PagePlaceholder
      title="Login"
      description="Authentication page placeholder for sign in, sign up, and future social auth options."
    />
  )
}
